# Штамп без прозрачности (Вариант A)

Коротко: теперь текстовая метка в правом верхнем углу вставляется как небольшой штамп (только текст, шрифт Helvetica) без прозрачности и без вмешательства в исходный контент страниц. Это даёт предсказуемое отображение и печать, без «кракозябр» и пропадающих изображений.

## Что сделано

- Добавлен штамп как мини-страница (Form) с текстом `Helvetica` (без встраивания TTF).
- Точное позиционирование штампа в правом верхнем углу с учётом `/CropBox` и `/Rotate` (0/90/180/270).
- Вставка через `merge_transformed_page` (PyPDF2 `Transformation`), а не «полноразмерный» оверлей.
- Старый путь через `create_overlay` оставлен как fallback, но больше не используется в основной логике.

Задействованный код: `pdf_attachments_ui.py`
- Импортирован `Transformation` из `PyPDF2`.
- Добавлены функции: `_create_stamp_page`, `_visible_box`, `_anchor_and_angle`, `_merge_stamp_top_right`.
- Функции вставки текста (`insert_text_to_pdf`, ветка слияния в `create_merged_pdf`) переводены на новый способ.

## Как это работает

1) Определяем видимую область страницы: берётся `/CropBox` (если есть), иначе `/MediaBox`.
2) Считываем `/Rotate` и вычисляем визуальный «правый верхний» якорь:
   - 0°: (right - margin, top - margin)
   - 90°: (left + margin, top - margin)
   - 180°: (left + margin, bottom + margin)
   - 270°: (right - margin, bottom + margin)
3) Готовим штамп фиксированного размера (по умолчанию 200×40 pt), текст рисуем выравниванием по правому краю в точке `(stamp_width, 0)` — это якорь штампа.
4) Применяем матрицу преобразования `Transformation().rotate(deg).translate(tx, ty)` так, чтобы точка `(stamp_width, 0)` перешла в вычисленный якорь страницы `(ax, ay)`.

Формулы для смещения (в градусах `deg`, радианы `rad`, `cos=cos(rad)`, `sin=sin(rad)`):

```
tx = ax - (cos * stamp_width + sin * 0)
ty = ay - (-sin * stamp_width + cos * 0)
```

Это соответствует PDF-матрице поворота `[[cos, sin], [-sin, cos]]` и пост-переносу `T`.

## Почему это надёжнее

- Нет прозрачности/SoftMask/Blend → исключаем рендер/печать артефакты.
- `Helvetica` — базовый шрифт PDF, не требует встраивания и минимизирует конфликты `/Resources`.
- Штамп аккуратно «кладётся сверху» без перепаковки исходного потока и без перезаписи имён ресурсов страницы.
- Печатается везде одинаково (в отличие от аннотаций, которые могут не печататься без «Print comments»).

## Аннотации vs. штамп

- Аннотации (`FreeText`) подходят для интерактивных комментариев, но зависят от ридера и настроек печати.
- Типовая «мировая практика» для обязательных служебных меток — рисовать контент-штамп (overlay) прямо в поток страницы, с учётом `CropBox/Rotate`.

## Настройки/параметры

- Отступ `margin`: 12 pt (можно изменить в `_anchor_and_angle`).
- Размер штампа: 200×40 pt (можно изменить в `_create_stamp_page`).
- Шрифт: `Helvetica`, размер 12 pt (можно изменить в `_create_stamp_page`).

## Обновления/чистка кода (последние изменения)

- Убраны устаревшие таргетные фиксы под Letter 612×792 @ Rotate=270 в двух местах:
  - В `_anchor_and_angle` удалён блок «Ensure top-left handle for 270° landscape pages (fix for Letter @270)».
  - В `_merge_stamp` удалены два блока «Targeted fix … 612×792 @ 270 …».
- Вместо них действует обобщённое правило в конце `_anchor_and_angle`: если страница имеет `Rotate=270` и базовую портретную геометрию (видимая ширина < высоты), штамп якорится в визуальном верхнем правом углу. Это предотвращает уход штампа за правую границу.
- Добавлен параметр `visible_w270` (внутренняя переменная) для явного использования «визуальной ширины» при `Rotate=270` при вычислении `ay`.
- Удалены закомментированные и отключённые (if False …) участки, чтобы снизить путаницу и упростить поддержку.


## Что ещё пробовали (две стратегии)

Контекст: на «Странице 2» документа `att.1_P 192-22.pdf` базовый размер страницы — 612×792 pt (Letter, портрет), но в метаданных стоит `Rotate=270°`. Визуально страница выглядит альбомной 792×612, из‑за чего штамп, рассчитанный по базовым координатам, «уезжает».

Мы проверили два альтернативных подхода:

1) Нормализация страниц (Variant 1)
- Идея: физически повернуть содержимое страницы матрицей преобразования и сбросить `/Rotate` в 0. При поворотах 90/270 меняются местами ширина/высота (`MediaBox`/`CropBox`).
- Плюсы: после нормализации все страницы «прямые», координаты интуитивны; дальнейшая логика штампа не меняется.
- Минусы: это изменяет исходный поток страницы; на PDF с нестандартными ресурсами может потребоваться дополнительное тестирование.

Принцип (в терминах матриц PDF):
- 90°: `[0 1 -1 0 H 0]`, затем `W↔H`;
- 180°: `[-1 0 0 -1 W H]`;
- 270°: `[0 -1 1 0 0 W]`, затем `W↔H`;
и удалить ключ `/Rotate`.

2) Корректная трансформация координат (Variant 2)
- Идея: не менять страницу, а переводить «визуальные» координаты в PDF‑координаты с учётом `/Rotate`.
- Эффективные размеры: при 90/270 меняем местами ширину/высоту, работаем как будто страница `effW×effH`.
- Формулы преобразования (из визуальных в PDF):
  - rot=0: `(x, y)` → `(x, y)`
  - rot=90: `(x, y)` → `(y, W - x)`
  - rot=180: `(x, y)` → `(W - x, H - y)`
  - rot=270: `(x, y)` → `(H - y, x)`
- Плюсы: не меняем оригинальные страницы; можно адресно лечить «проблемные» страницы (например, только `Rotate=270`).
- Минусы: штамп нужно поворачивать на тот же угол, а расчёты вести аккуратно (учитывать `CropBox`).

Статус: оба подхода рабочие; файлы с прототипами (v1/v2) были удалены, но логику можно встроить прямо в текущий код:
- Вариант 1 — вызвать «нормализацию» перед `_merge_stamp`.
- Вариант 2 — оставить страницы как есть, а в `_merge_stamp` использовать визуальные координаты и формулы выше; для «Страницы 2» `att.1_P 192-22.pdf` (Rotate=270) это устраняет смещение.

Рекомендации:
- Для единообразной печати/сравнения разнородных PDF (A4/Letter, смешанные повороты) лучше предварительно нормализовать (Variant 1).
- Если важна минимальная инвазивность исходников, используйте координатную трансформацию (Variant 2) и ставьте штамп в визуальные углы страницы.
